func main() {
	var numero int
	fmt.Println("Digite o número inteiro:")
	fmt.Scan(&numero)

	sucessor := numero + 1
	antecessor := numero - 1

	fmt.Println("O sucessor de", numero, "é", sucessor)
	fmt.Println("O antecessor de", numero, "é", antecessor)
}

________________________________

Ler salário
Se (salário < 1000)
  aumento = salário * 0.1
Senão
  aumento = salário * 0.05
Fim Se
novo_salário = salário + aumento
Escrever "O novo salário é: " novo_salário
